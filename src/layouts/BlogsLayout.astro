---
import BaseLayout from "./BaseLayout.astro"

const { frontmatter } = Astro.props
---

<BaseLayout>
  <h1>{frontmatter.title}</h1>
  <p class="font-bold text-gray-500">Written on: {frontmatter.pubDate.toString().slice(0,10)}</p>
  <slot />
</BaseLayout>
